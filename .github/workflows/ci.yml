name: Node CI
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run custom NPM pipeline
        uses: ./.github/actions/npm-pipeline
        with:
          node-version: 20
